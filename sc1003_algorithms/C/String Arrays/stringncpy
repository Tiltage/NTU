    int i,len,count=0;
    len=strlen(s2);
    for (i=0;i<len;i++)
    {
        if (count!=n)
        {
            *(s1+i)=*(s2+i);
            count++;
        }
        else
        {
            break;
        }
    }
    *(s2+i)='\0';

    return s1;
