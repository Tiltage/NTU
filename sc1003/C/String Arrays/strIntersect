   int i,j,k=0,len1,len2,count=0;
   len1=strlen(str1);
   len2=strlen(str2);
   for (i=0;i<len1;i++)
   {
       for (j=0;j<len2;j++)
       if (*(str1+i)==*(str2+j))
       {
           *(str3+k)=*(str2+j);
           count++;
           k++;
           break;
       }
   }
    *(str3+k)='\0';
