int row,col,i=1,temp;
for (row=0;row<rowSize-1;row++)
{
    for (col=i;col<colSize;col++)
    {
        temp=ar[row][col];
        ar[row][col]=ar[col][row];
        ar[col][row]=temp;
    }
    i++;
}
